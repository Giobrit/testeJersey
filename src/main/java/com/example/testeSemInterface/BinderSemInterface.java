package com.example.testeSemInterface;

import com.google.inject.AbstractModule;

public class BinderSemInterface extends AbstractModule {
    @Override
    public void configure() {
//        bind(Soma.class);
    }
}
